# ✨ Premium Git Bash Commands 

---

## 🟦 **Basic Git Commands**
> ```
> ┌──────────────────────────────────────────────────────────┐
> │ 1. git init                                              │
> │ 2. git status                                            │
> │ 3. git add <file>                                        │
> │ 4. git add .                                             │
> │ 5. git commit -m "message"                               │
> │ 6. git config --global user.name "Your Name"             │
> │ 7. git config --global user.email "you@example.com"      │
> │ 8. git clone <repo-url>                                  │
> │ 9. git remote -v                                         │
> │ 10. git remote add origin <url>                          │
> └──────────────────────────────────────────────────────────┘
> ```

---

## 🟩 **Branching Commands**
> ```
> ┌──────────────────────────────────────────────────────────┐
> │ 11. git branch                                           │
> │ 12. git branch <branch-name>                             │
> │ 13. git checkout <branch-name>                           │
> │ 14. git checkout -b <branch-name>                        │
> │ 15. git branch -d <branch-name>                          │
> │ 16. git branch -D <branch-name>                          │
> └──────────────────────────────────────────────────────────┘
> ```

---

## 🟧 **Merging & Rebasing**
> ```
> ┌──────────────────────────────────────────────────────────┐
> │ 17. git merge <branch-name>                              │
> │ 18. git rebase <branch-name>                             │
> │ 19. git rebase --continue                                │
> │ 20. git rebase --abort                                   │
> └──────────────────────────────────────────────────────────┘
> ```

---

## 🟪 **Logs & History**
> ```
> ┌──────────────────────────────────────────────────────────┐
> │ 21. git log                                              │
> │ 22. git log --oneline                                    │
> │ 23. git log --graph --oneline --decorate                 │
> │ 24. git diff                                             │
> │ 25. git diff --staged                                    │
> │ 26. git show <commit-id>                                 │
> └──────────────────────────────────────────────────────────┘
> ```

---

## 🟨 **Stashing Commands**
> ```
> ┌──────────────────────────────────────────────────────────┐
> │ 27. git stash                                            │
> │ 28. git stash list                                       │
> │ 29. git stash apply                                      │
> │ 30. git stash drop                                       │
> └──────────────────────────────────────────────────────────┘
> ```

---

## 🟥 **Pull, Push, Fetch**
> ```
> ┌──────────────────────────────────────────────────────────┐
> │ 31. git push origin <branch>                             │
> │ 32. git pull origin <branch>                             │
> │ 33. git fetch origin                                     │
> └──────────────────────────────────────────────────────────┘
> ```

---

## 🟫 **Undo / Reset**
> ```
> ┌──────────────────────────────────────────────────────────┐
> │ 34. git reset --soft <commit>                            │
> │ 35. git reset --hard <commit>                            │
> │ 36. git checkout -- <file>                               │
> └──────────────────────────────────────────────────────────┘
> ```

---

## ⬛ **Tagging**
> ```
> ┌──────────────────────────────────────────────────────────┐
> │ 37. git tag                                              │
> │ 38. git tag <tagname>                                    │
> │ 39. git push origin --tags                               │
> └──────────────────────────────────────────────────────────┘
> ```

---