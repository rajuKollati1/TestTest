apiVersion: v1
kind: Service
metadata:
  name: {{ include "venky.fullname" . }}
  labels:
    {{- include "venky.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "venky.selectorLabels" . | nindent 4 }}
