apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "venky.fullname" . }}-test-connection"
  labels:
    {{- include "venky.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "venky.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
