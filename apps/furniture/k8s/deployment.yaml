apiVersion: apps/v1
kind: Deployment
metadata:
  name: furniture-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: furniture-app
  template:
    metadata:
      labels:
        app: furniture-app
    spec:
      containers:
      - name: furniture-app
        image: rajuprudhvi/myapp:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: furniture-service
spec:
  type: NodePort
  selector:
    app: furniture-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30010