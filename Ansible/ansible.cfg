[defaults]
inventory = inventory.ini
# This disables the "Accept SSH fingerprint" prompt for new lab hosts
host_key_checking = False
# Makes output more readable
stdout_callback = default
interpreter_python = auto_silent

[callback_default]
result_format = yaml