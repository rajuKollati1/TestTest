SECRET_NAME="grafana"
NAMESPACE="default"
PASSWORD_KEY="admin-password"

# Fetch the password value, decode it from Base64, and display it
kubectl get secret $SECRET_NAME -n $NAMESPACE -o json | \
  jq -r ".data.\"$PASSWORD_KEY\"" | \
  base64 --decode